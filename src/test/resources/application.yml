server:
  port: 0

spring:
  application:
    name: TaskBoard.Service.Boot

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://fake-issuer
          jwk-set-uri: http://fake-issuer/certs
